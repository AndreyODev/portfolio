/**
 * Tokens de layout — fonte única para padding, altura e largura das seções.
 */
export const layout = {
  headerHeight: '4rem',
  maxWidth: '80rem',
  paddingX: 'clamp(1rem, 5vw, 5rem)',
  paddingY: 'clamp(1rem, 3vh, 2rem)',
  gap: 'clamp(1.5rem, 4vw, 3rem)',
} as const
